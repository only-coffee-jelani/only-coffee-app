{
  "name": "kinesis-event-processor",
  "version": "1.0.0",
  "description": "AWS Lambda function to process user events from Kinesis Data Stream",
  "main": "index.js",
  "scripts": {
    "deploy": "sh deploy.sh"
  },
  "dependencies": {
    "pg": "^8.12.0",
    "@google-cloud/bigquery": "^7.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
