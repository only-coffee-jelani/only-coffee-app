/**
 * StoreType Enum
 * Represents store types in the system
 * These correspond to the codes in the store_types lookup table
 */
export enum StoreType {
  COFFEE_SHOP = 'coffee_shop',
  FOOD_TRUCK = 'food_truck',
  KIOSK = 'kiosk',
  POPUP = 'popup',
}

