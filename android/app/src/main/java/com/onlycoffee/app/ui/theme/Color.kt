package com.onlycoffee.app.ui.theme

import androidx.compose.ui.graphics.Color

// Brand Colors - Only Coffee Coral Pink Theme (#ff93a3)
val BrandPrimary = Color(0xFFFF93A3) // Main Coral Pink Color
val BrandSecondary = Color(0xFFFF93A3) // Same Coral Pink
val BrandAccent = Color(0xFFFF93A3) // Same Coral Pink

// Background Colors
val BackgroundPrimary = Color(0xFFFFFFFF) // Pure White
val BackgroundSecondary = Color(0xFFFFFFFF) // Pure White
val CardBackground = Color(0xFFFFFFFF) // Pure White

// Text Colors
val TextPrimary = Color(0xFF000000) // Pure Black
val TextSecondary = Color(0xFF000000) // Pure Black
val TextTertiary = Color(0xFF000000) // Pure Black
val TextOnPrimary = Color(0xFFFFFFFF) // White on coral pink
val TextOnSecondary = Color(0xFF000000) // Black on white

// UI Colors
val BorderLight = Color(0xFFFF93A3) // Coral Pink Border
val BorderMedium = Color(0xFFFF93A3) // Coral Pink Border
val BorderDark = Color(0xFFFF93A3) // Coral Pink Border

// Interactive Colors
val ButtonPrimary = BrandPrimary
val ButtonSecondary = Color(0xFFFFFFFF) // White buttons with coral pink text
val ButtonDisabled = Color(0xFFE0E0E0) // Light gray for disabled
val ButtonText = Color(0xFFFFFFFF) // White text on coral pink buttons

// Status Colors
val StatusSuccess = Color(0xFF4CAF50) // Green
val StatusWarning = Color(0xFFFF9800) // Orange
val StatusError = Color(0xFFF44336) // Red
val StatusInfo = Color(0xFF2196F3) // Blue

// Rating Colors
val RatingGold = Color(0xFFFF93A3) // Coral Pink for ratings
val RatingEmpty = Color(0xFFE0E0E0) // Light Gray for empty ratings

// Shadow Colors
val ShadowLight = Color(0x0A000000) // 4% Black
val ShadowMedium = Color(0x14000000) // 8% Black
val ShadowDark = Color(0x1F000000) // 12% Black

// Dark Theme Colors (for future implementation)
val DarkBackgroundPrimary = Color(0xFF000000) // Pure Black
val DarkBackgroundSecondary = Color(0xFF1A1A1A) // Dark Gray
val DarkCardBackground = Color(0xFF2A2A2A) // Card Dark
val DarkTextPrimary = Color(0xFFFFFFFF) // White
val DarkTextSecondary = Color(0xFFE0E0E0) // Light Gray
val DarkTextTertiary = Color(0xFFB0B0B0) // Medium Gray

// Gradient Colors
val GradientStart = BrandPrimary
val GradientEnd = BrandPrimary // Same coral pink for consistent look

// Overlay Colors
val OverlayLight = Color(0x33000000) // 20% Black
val OverlayMedium = Color(0x66000000) // 40% Black
val OverlayDark = Color(0x99000000) // 60% Black

// Special Colors - All replaced with coral pink
val WaffolinoGold = Color(0xFFFF93A3) // Coral Pink for Waffolino branding
val CoffeeBean = Color(0xFFFF93A3) // Coral Pink instead of brown
val Espresso = Color(0xFFFF93A3) // Coral Pink instead of brown
val Latte = Color(0xFFFFFFFF) // White for light drinks
val Cappuccino = Color(0xFFFF93A3) // Coral Pink for consistency

// Status Color Variants
val SuccessLight = Color(0xFFE8F5E9) // Light green background
val SuccessDark = Color(0xFF2E7D32) // Dark green text
val InfoLight = Color(0xFFE3F2FD) // Light blue background
val InfoDark = Color(0xFF1565C0) // Dark blue text
val WarningLight = Color(0xFFFFF3E0) // Light orange background
val WarningDark = Color(0xFFE65100) // Dark orange text
val ErrorLight = Color(0xFFFFEBEE) // Light red background
val ErrorDark = Color(0xFFC62828) // Dark red text
